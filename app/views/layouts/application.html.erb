<!DOCTYPE html>
<html>
<head>
  <title><%= if @title then "#{@title} // " end %><%= $site_title %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <script src="https://<%= $socket_host %>/socket.io/socket.io.js" charset="utf-8"></script>
  <%= csrf_meta_tags %>
</head>
<body data-no-turbolink>
  <div class="content">
    <header>
      <div class="header-inner">
        <%= link_to "CAH Creator", root_path %>
        <div class="header-pull-right">
          <% if current_user %>
            <%= link_to "New Deck", new_deck_path %> //
            <%= link_to "Import Deck", decks_import_path %> //
            <%= link_to "My Account", current_user %> //
            <% unless current_user.premium %>
              <%= link_to "Get Premium", premium_path %> //
            <% end %>
            <% if current_user.admin %>
              <%= link_to "Admin", rails_admin_path %> //
            <% end %>
            <%= link_to "Log Out", logout_path %>
          <% else %>
            <%= link_to "Log In", auth_path %>
          <% end %>
        </div>
      </div>
    </header>
    <div class="content-body">
      <div class="notice">
        <b>Global Notice:</b> CAH Creator doesn't appear to be working with
        Cards Against Equestria. I've sent a pull request over to Rylius that
        will hopefully fix it.
      </div>
      <%= yield %>
    </div>
    <footer>
      <div class="footer-inner">
        &copy; TJ Horner <%= link_to "and contributors", "https://github.com/CAHCreator/cah-creator/graphs/contributors" %> 2015-2016 //
        <%= link_to "Donate", donate_path %>
      </div>
    </footer>
    <script>
      // GA
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-78784564-1', 'auto');
      ga('send', 'pageview');
    </script>
    <% if flash[:error] %>
      <script>alert("Error: <%= j flash[:error] %>")</script>
    <% end %>
  </div>
</body>
</html>
